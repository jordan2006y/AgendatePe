package com.example.agendatepe.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Principal
val MainBlue = Color(0xFF0056D2)
val MainBlack = Color(0xFF121212)
val MainWhite = Color(0xFFFFFFFF)
val MainGray = Color(0xFFF5F5F5)

// Textos
val TextBlack = Color(0xFF1A1C1E)
val TextGray = Color(0xFF757575)

// --- MODO OSCURO TIPO INSTAGRAM ---
val DarkBackground = Color(0xFF000000) // Negro Puro (Fondo)
val DarkSurface = Color(0xFF121212)    // Gris Muy Oscuro (Tarjetas/Inputs)

// Efecto Glass
val GlassBlue = MainBlue.copy(alpha = 0.85f)
val GlassBorder = Color.White.copy(alpha = 0.3f)

// Compatibilidad
val Purple80 = MainBlue
val PurpleGrey80 = MainBlack
val Pink80 = MainBlue
val Purple40 = MainBlue
val PurpleGrey40 = MainBlack
val Pink40 = MainBlue

val black = MainBlack
val Azul = MainBlue
val Crema = Color(0xFF00C4B4)
val SelectedField = Color(0xFFE3F2FD)
val UnselectedField = MainWhite
val BackgroundButton = MainBlue
val shapeButton = Color.White